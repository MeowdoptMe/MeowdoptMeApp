const databaseUrl = 'http://localhost:8000/';
const loginUrl = databaseUrl + 'user-auth/login/';
const registerUrl = databaseUrl + 'user-auth/register/';

const Database = {databaseUrl, loginUrl, registerUrl};

export default Database;
